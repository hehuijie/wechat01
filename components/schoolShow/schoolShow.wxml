<view class='schoolVal'>
<block wx:if='{{ownSchoolArr.length <=0}}'>请选择校区</block>
<block wx:for='{{ownSchoolArr}}'>
  <block wx:if='{{index == ownSchoolArr.length-1}}'>
  {{item.schoolName}}
  </block>
  <block wx:else>
  {{item.schoolName}}、
  </block>
</block>
</view>